services:
  paneo:
    build:
      context: .
      target: dev
    image: ghcr.io/codewec/paneo:dev
    container_name: paneo-dev
    user: "${UID}:${GID}"
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - ./:/app
      - /app/node_modules
      - /app/.nuxt
      - ${PANEO_DATA_DIR}:/app/.paneo
      - ${PANEO_SOURCE_1}:/data/source-1
      - ${PANEO_SOURCE_2}:/data/source-2
    command: pnpm dev --host 0.0.0.0 --port 3000
